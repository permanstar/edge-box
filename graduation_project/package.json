{
  "name": "edge-box-management",
  "version": "1.0.0",
  "description": "边缘盒子管理系统",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "start-client": "node mqtt-client.js",
    "dev": "concurrently \"npm run start\" \"npm run start-client\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "edge-box-management": "file:",
    "express": "^4.18.2",
    "mqtt": "^5.0.3",
    "ws": "^8.13.0",
    "sqlite3": "^5.1.6",
    "better-sqlite3": "^8.5.1"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
